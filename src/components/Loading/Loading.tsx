import React from "react";
import { Container, Spinner } from "./Loading.styled";

function Loading() {
  return (
    <Container>
      <Spinner></Spinner>
    </Container>
  );
}

export default Loading;
